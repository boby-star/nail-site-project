import Image from "next/image";import { desc } from "drizzle-orm";import { db } from "@/db";import { media } from "@/db/schema";import { AdminShell } from "@/components/admin/admin-shell";import { requireAdmin } from "@/modules/auth/service";import { uploadMedia } from "@/modules/media/actions";
export const dynamic="force-dynamic";
export default async function Page({searchParams}:{searchParams:Promise<{uploaded?:string;error?:string}>}){const user=await requireAdmin(),items=await db.select().from(media).orderBy(desc(media.createdAt));const state=await searchParams;return <AdminShell user={user}><header className="admin-page-header"><div><span className="eyebrow">Бібліотека файлів</span><h1>Медіа</h1><p>JPEG або PNG, до 10 МБ. Для кожного зображення обов’язковий альтернативний текст.</p></div></header>{state.uploaded&&<p className="notice success">Зображення завантажено.</p>}{state.error&&<p className="notice error">Не вдалося завантажити файл. Перевірте формат, розмір і поля.</p>}<form action={uploadMedia} className="admin-card admin-form" encType="multipart/form-data"><h2>Завантажити зображення</h2><label>Файл<input type="file" name="file" accept="image/jpeg,image/png" required/></label><label>Альтернативний текст<input name="alt" maxLength={300} required placeholder="Опишіть, що зображено"/></label><label>Підпис<textarea name="caption" maxLength={1000} rows={2}/></label><button className="button" type="submit">Завантажити</button></form><div className="media-grid">{items.map(item=><article className="media-card" key={item.id}><Image src={item.storagePath} alt={item.alt} width={item.width} height={item.height}/><div><strong>{item.alt}</strong><small>{item.width} × {item.height} · {Math.ceil(item.size/1024)} КБ</small>{item.caption&&<p>{item.caption}</p>}</div></article>)}</div>{!items.length&&<p className="empty">Зображень поки немає.</p>}</AdminShell>}
