import { asc } from "drizzle-orm";import { db } from "@/db";import { cities } from "@/db/schema";import { AdminShell } from "@/components/admin/admin-shell";import { requireAdmin } from "@/modules/auth/service";import { createCity,deleteCity } from "@/modules/taxonomy/actions";
export const dynamic="force-dynamic";export default async function Page({searchParams}:{searchParams:Promise<{created?:string;error?:string}>}){const user=await requireAdmin(),items=await db.select().from(cities).orderBy(asc(cities.name)),state=await searchParams;return <AdminShell user={user}><header className="admin-page-header"><div><span className="eyebrow">Локальні сторінки</span><h1>Міста</h1></div></header>{state.created&&<p className="notice success">Місто створено.</p>}{state.error&&<p className="notice error">Перевірте поля. Slug має бути унікальним.</p>}<form action={createCity} className="admin-card admin-form"><h2>Нове місто</h2><div className="form-grid"><label>Назва<input name="name" maxLength={120} required/></label><label>Slug<input name="slug" pattern="[a-z0-9]+(?:-[a-z0-9]+)*" required/></label><label>Область<input name="region" maxLength={160}/></label></div><label>Опис<textarea name="description" rows={3} maxLength={3000}/></label><button className="button" type="submit">Створити місто</button></form><div className="admin-card"><h2>Усі міста</h2>{items.map(item=><div className="list-row" key={item.id}><div><strong>{item.name}</strong><small>{item.region} · /{item.slug}</small></div><form action={deleteCity}><input type="hidden" name="id" value={item.id}/><button className="danger" type="submit">Видалити</button></form></div>)}{!items.length&&<p>Міст поки немає.</p>}</div></AdminShell>}
