import { asc } from "drizzle-orm";import { db } from "@/db";import { categories } from "@/db/schema";import { AdminShell } from "@/components/admin/admin-shell";import { requireAdmin } from "@/modules/auth/service";import { createCategory,deleteCategory } from "@/modules/taxonomy/actions";
export const dynamic="force-dynamic";export default async function Page({searchParams}:{searchParams:Promise<{created?:string;error?:string}>}){const user=await requireAdmin(),items=await db.select().from(categories).orderBy(asc(categories.name)),state=await searchParams;return <AdminShell user={user}><header className="admin-page-header"><div><span className="eyebrow">Структура матеріалів</span><h1>Категорії</h1></div></header>{state.created&&<p className="notice success">Категорію створено.</p>}{state.error&&<p className="notice error">Перевірте поля. Slug має бути унікальним.</p>}<form action={createCategory} className="admin-card admin-form"><h2>Нова категорія</h2><div className="form-grid"><label>Назва<input name="name" maxLength={120} required/></label><label>Slug<input name="slug" pattern="[a-z0-9]+(?:-[a-z0-9]+)*" required/></label></div><label>Опис<textarea name="description" rows={3} maxLength={3000}/></label><button className="button" type="submit">Створити категорію</button></form><div className="admin-card"><h2>Усі категорії</h2>{items.map(item=><div className="list-row" key={item.id}><div><strong>{item.name}</strong><small>/{item.slug}</small></div><form action={deleteCategory}><input type="hidden" name="id" value={item.id}/><button className="danger" type="submit">Видалити</button></form></div>)}{!items.length&&<p>Категорій поки немає.</p>}</div></AdminShell>}
