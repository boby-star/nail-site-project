import type { Metadata } from "next";import { loginAction } from "../actions";export const metadata:Metadata={title:"Вхід до керування",robots:{index:false,follow:false}};const messages:Record<string,string>={fields:"Перевірте виділені поля.",credentials:"Ім’я користувача або пароль неправильні.",limit:"Забагато спроб. Спробуйте трохи пізніше."};export default async function Page({searchParams}:{searchParams:Promise<{error?:string}>}){const error=(await searchParams).error;return <main className="admin-login"><form action={loginAction} className="card"><h1>Вхід до керування</h1><p>Введіть дані адміністратора, створеного на сервері.</p>{error&&<p role="alert">{messages[error]??"Не вдалося увійти."}</p>}<label>Ім’я користувача<input name="username" autoComplete="username" required minLength={3}/></label><label>Пароль<input name="password" type="password" autoComplete="current-password" required minLength={12}/></label><button className="button" type="submit">Увійти</button></form></main>}
