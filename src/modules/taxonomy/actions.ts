"use server";
import { eq } from "drizzle-orm";import { revalidatePath } from "next/cache";import { redirect } from "next/navigation";import { z } from "zod";import { db } from "@/db";import { auditLogs,categories,cities } from "@/db/schema";import { slugSchema } from "@/lib/validation/forms";import { requireAdmin } from "@/modules/auth/service";
const base=z.object({name:z.string().trim().min(1).max(120),slug:slugSchema,description:z.string().trim().max(3000).optional()});
export async function createCategory(form:FormData){const admin=await requireAdmin();const parsed=base.safeParse({name:form.get("name"),slug:form.get("slug"),description:form.get("description")||undefined});if(!parsed.success)redirect("/admin/categories?error=1");const [item]=await db.insert(categories).values(parsed.data).returning({id:categories.id});await db.insert(auditLogs).values({adminId:admin.id,action:"CATEGORY_CREATED",entityType:"category",entityId:item.id});revalidatePath("/admin/categories");redirect("/admin/categories?created=1")}
export async function deleteCategory(form:FormData){const admin=await requireAdmin();const id=String(form.get("id"));await db.delete(categories).where(eq(categories.id,id));await db.insert(auditLogs).values({adminId:admin.id,action:"CATEGORY_DELETED",entityType:"category",entityId:id});revalidatePath("/admin/categories")}
export async function createCity(form:FormData){const admin=await requireAdmin();const parsed=base.extend({region:z.string().trim().max(160).optional()}).safeParse({name:form.get("name"),slug:form.get("slug"),region:form.get("region")||undefined,description:form.get("description")||undefined});if(!parsed.success)redirect("/admin/cities?error=1");const [item]=await db.insert(cities).values(parsed.data).returning({id:cities.id});await db.insert(auditLogs).values({adminId:admin.id,action:"CITY_CREATED",entityType:"city",entityId:item.id});revalidatePath("/admin/cities");redirect("/admin/cities?created=1")}
export async function deleteCity(form:FormData){const admin=await requireAdmin();const id=String(form.get("id"));await db.delete(cities).where(eq(cities.id,id));await db.insert(auditLogs).values({adminId:admin.id,action:"CITY_DELETED",entityType:"city",entityId:id});revalidatePath("/admin/cities")}
